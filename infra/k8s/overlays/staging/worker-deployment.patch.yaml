apiVersion: apps/v1
kind: Deployment
metadata:
  name: phynd-crm-worker
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: worker
          envFrom:
            - secretRef:
                name: phynd-crm-staging-secrets
            - secretRef:
                name: phynd-acca-staging-secrets
                optional: true
